# Copia este archivo a .env y ajusta si cambias las credenciales del docker-compose
DATABASE_URL="postgresql://admin:admin123@localhost:5433/trabajo_grado?schema=public"

# Clave para firmar/verificar tokens JWT (modulo Auth). Genera una propia con:
#   python3 -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET="cambia-esto-por-una-clave-larga-y-aleatoria"
